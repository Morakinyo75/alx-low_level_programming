#include "main.h"

/**
 * swap_int - swaps two integers values
 * @a: first integer
 * @c: second integer
 * Return: nothing
 */

void swap_int(int *a, int *c)
{
	int tmp = *a;

	*a = *c;
	*b = tmp;
}
